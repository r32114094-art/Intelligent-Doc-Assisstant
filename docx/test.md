# RAG 评测数据集

> 本文件为智能文档问答助手的标准测试集，用于自动化 RAG 管道评估。
> 格式规范：每条用例包含 `Q:` (问题)、`A:` (标准答案)、`S:` (来源章节)。

---

## 简单问题

### Q1
- **Q:** 什么是NLP？
- **A:** NLP 是一种让计算机理解、解释和生成人类语言的技术。它是人工智能领域中一个极为活跃和重要的研究方向，其核心任务是通过计算机程序来模拟人类对语言的认知和使用过程。NLP 结合了计算机科学、人工智能、语言学和心理学等多个学科的知识和技术，旨在打破人类语言和计算机语言之间的障碍，实现无缝的交流与互动。NLP技术使得计算机能够执行各种复杂的语言处理任务，如中文分词、子词切分、词性标注、文本分类、实体识别、关系抽取、文本摘要、机器翻译、自动问答等。这些任务不仅要求计算机能够识别和处理语言的表层结构，更重要的是可以理解语言背后的深层含义，包括语义、语境、情感和文化等方面的复杂因素。随着深度学习等现代技术的发展，NLP 已经取得了显著的进步。通过训练大量的数据，深度学习模型能够学习到语言的复杂模式和结构，从而在多个 NLP 任务上取得了接近甚至超越人类水平的性能。然而，尽管如此，NLP 仍然面临着诸多挑战，如处理歧义性、理解抽象概念、处理隐喻和讽刺等。研究人员正致力于通过更加先进的算法、更大规模的数据集和更精细的语言模型来解决这些问题，以推动NLP技术不断发展。
- **S:** 第1章 1.1什么是NLP

### Q2
- **Q:** 什么是 Seq2Seq 模型？ Encoder 和 Decoder 内部传统神经网络的经典结构是什么？
- **A:** Seq2Seq，即序列到序列，是一种经典 NLP 任务。具体而言，是指模型输入的是一个自然语言序列，输出的是一个可能不等长的自然语言序列。事实上，Seq2Seq 是 NLP 最经典的任务，几乎所有的 NLP 任务都可以视为 Seq2Seq 任务。例如文本分类任务，可以视为输出长度为 1 的目标序列；词性标注任务，可以视为输出与输入序列等长的目标序列。Encoder 和 Decoder 内部传统神经网络的经典结构是前馈神经网络（FNN）、层归一化（Layer Norm）和残差连接（Residual Connection）。
- **S:** 第2章 > 2.2.1 Seq2Seq模型

### Q3
- **Q:** WordPiece是什么？
- **A:** WordPiece 是另一种基于子词的分词方法，最初用于谷歌的 BERT 模型。与 BPE 类似，WordPiece 通过最大化子词序列的似然函数来生成词典，但在合并子词时更注重语言模型的优化。WordPiece 会优先选择能够最大化整体句子概率的子词，使得分词结果在语言模型中具有更高的概率。
- **S:** 第5章 5.2.3 Subword Tokenizer

### Q4
- **Q:** RLHF是什么？
- **A:** RLHF，全称是 Reinforcement Learning from Human Feedback，即人类反馈强化学习，是利用强化学习来训练 LLM 的关键步骤。相较于在 GPT-3 就已经初见雏形的 SFT，RLHF 往往被认为是 ChatGPT 相较于 GPT-3 的最核心突破。事实上，从功能上出发，我们可以将 LLM 的训练过程分成预训练与对齐（alignment）两个阶段。预训练的核心作用是赋予模型海量的知识，而所谓对齐，其实就是让模型与人类价值观一致，从而输出人类希望其输出的内容。在这个过程中，SFT 是让 LLM 和人类的指令对齐，从而具有指令遵循能力；而 RLHF 则是从更深层次令 LLM 和人类价值观对齐，令其达到安全、有用、无害的核心标准。
- **S:** 第四章 4.2.3 RLHF

### Q5
- **Q:** 为什么要进行模型预训练？
- **A:** 手写实现 LLM 结构工作量大，难以实时跟进最新模型的结构创新；从零实现的 LLM 训练无法较好地实现多卡分布式训练，训练效率较低；和现有预训练 LLM 不兼容，无法使用预训练好的模型参数。
- **S:** 第六章 6.1模型预训练

---

## 中等问题

### Q6
- **Q:** 在SFT（有监督微调）的数据处理中，loss_mask的作用是什么？如何生成？
- **A:** loss_mask用于标记序列中哪些位置需要计算损失（1表示计算，0表示不计算）。在SFT中，只有模型需要生成的回答（Assistant部分）参与损失计算，用户指令（User/Human部分）不计算。生成规则：当token序列中出现<|im_start|>assistant\n时，从该子序列结束位置开始标记为1，直到遇到<|im_end|>为止，其余位置标记为0。
- **S:** 第5.3.3节 SFTDataset

### Q7
- **Q:** LLM Agent的核心能力有哪些？Tiny-Agent中如何实现工具调用？
- **A:** LLM Agent的核心能力包括：理解目标、自主规划、记忆、工具使用、反思与迭代。在Tiny-Agent中，工具调用通过以下步骤实现：将工具函数（如获取时间、计数、搜索等）转换为JSON Schema格式；在调用大模型时，将工具Schema传入tools参数；若模型返回tool_calls，Agent解析请求，执行对应的Python函数；将执行结果以tool角色消息返回给模型，模型据此生成最终回复。
- **S:** 第7.3.1节 什么是LLM Agent

### Q8
- **Q:** Transformer中为什么要使用多头注意力机制？简述其优势。
- **A:** 单一注意力机制只能拟合一种相关关系，难以全面捕捉语句序列中的复杂依赖。多头注意力机制同时进行多次注意力计算，每个注意力头可以学习不同的语义关系（如语法、指代、局部依赖等），最后将多个头的结果拼接并线性变换，从而更全面、深入地拟合语言信息。
- **S:** 第2.1.6节 多头注意力

### Q9
- **Q:** BERT使用了哪两种预训练任务？分别简述其目标。
- **A:** BERT使用了掩码语言模型（MLM）和下一句预测（NSP）。MLM：随机遮蔽输入序列中15%的token，要求模型预测被遮蔽的token，从而学习双向语义关系。NSP：输入一对句子，要求模型判断第二个句子是否为第一个句子的下一句，从而学习句子级别的语义关系。
- **S:** 第3.1.1节 预训练任务——MLM + NSP

### Q10
- **Q:** 为什么说 LLM 本质上仍然是一个"下一个 token 预测模型"？
- **A:** 因为 LLM 的训练目标仍然是：给定前文，预测下一个 token（CLM任务），即输入 x1, x2, ..., xt，预测 xt+1。即使是对话、多轮任务，本质仍是序列建模。这也是为什么可以统一处理翻译、问答、代码等任务，可以用prompt完成不同任务。
- **S:** 4.2.1 Pretrain

### Q11
- **Q:** NLP发展阶段是什么？
- **A:** 规则 → 统计 → 深度学习 → 大模型。
- **S:** 1.2 NLP发展历程

### Q12
- **Q:** 为什么 Transformer 是核心架构？
- **A:** 因为其具备全局建模能力 + 并行计算能力 + 可扩展性。
- **S:** 第2章 Transformer 架构

### Q13
- **Q:** LLM相比于传统PLM有哪些涌现能力？请列举至少三种。
- **A:** LLM具备以下涌现能力：上下文学习（In-context Learning）：通过自然语言指令或少量示例执行任务，无需参数更新。指令遵循（Instruction Following）：理解并执行未见过的自然语言指令，具有强泛化能力。逐步推理（Step-by-Step Reasoning）：通过思维链（Chain-of-Thought）等策略解决多步骤逻辑推理问题。
- **S:** 第4.1.2节 LLM的能力

### Q14
- **Q:** 在LLaMA2模型实现中，旋转位置嵌入（RoPE）是如何实现的？请简述 precompute_freqs_cis 函数的作用。
- **A:** RoPE通过对查询（Q）和键（K）的向量进行旋转编码来注入位置信息。precompute_freqs_cis 函数的作用是：生成频率序列（基于theta和维度）；创建时间序列 t；计算外积得到频率矩阵；返回频率的余弦值（实部）和正弦值（虚部），用于后续 apply_rotary_emb 函数中对 Q 和 K 进行旋转。
- **S:** 第5.1.3.2节 旋转嵌入

### Q15
- **Q:** 什么是BPE分词算法？它在RoBERTa中如何使用？
- **A:** BPE（Byte Pair Encoding）是一种子词切分算法，通过反复合并语料中频率最高的字符对或子词对来构建子词词典。RoBERTa使用了50K大小的BPE词表（相比BERT的30K WordPiece词表更大），从而优化了模型对未登录词和罕见词的处理能力。
- **S:** 第3.1.2节 优化三：更大的bpe词表

---

## 困难问题

### Q16
- **Q:** Transformer中的Pre-Norm与Post-Norm结构有何区别？在GPT、LLaMA、BERT和GLM中分别采用了哪种？请分析选择不同结构的可能原因。
- **A:** Pre-Norm：先进行层归一化（LayerNorm），再进行注意力或FFN计算，最后残差连接。公式为：x = x + SubLayer(LayerNorm(x))。Post-Norm：先进行子层计算，再进行残差连接，最后归一化。公式为：x = LayerNorm(x + SubLayer(x))。各模型采用情况：GPT（GPT-2起）、LLaMA系列：Pre-Norm（训练更稳定，适合深层模型）。BERT：Post-Norm（原始Transformer论文配图为Post-Norm，但BERT沿用）。GLM：Post-Norm（论文提出可避免数值错误，但主流LLM仍以Pre-Norm为主）。原因分析：Pre-Norm能缓解梯度消失/爆炸，适合超深网络；Post-Norm正则化更强，但对大模型训练稳定性要求高。
- **S:** 第2.2.4节 残差连接, 第2.2.5节 Encoder, 第3.1.1节 BERT模型架构, 第3.3.2节 LLaMA模型架构, 第3.3.3节 GLM

### Q17
- **Q:** LLaMA2模型在注意力机制上引入了分组查询注意力（GQA），相比于原始Transformer的多头注意力（MHA）和多查询注意力（MQA）有何优势？在代码实现中，repeat_kv函数的作用是什么？请结合注意力计算过程解释。
- **A:** MHA：每个头有独立的KV，参数量大，计算量大。MQA：所有头共享同一组KV，显存占用低，但质量可能下降。GQA：将头分为若干组，每组共享KV，平衡了质量和效率。LLaMA2-70B使用GQA。repeat_kv的作用：在GQA中，KV头的数量（n_kv_heads）通常小于查询头的数量（n_local_heads）。repeat_kv将KV张量沿头维度重复 n_rep = n_local_heads // n_kv_heads 次，使得KV与Q的头数一致，从而能够进行批量矩阵乘法计算注意力分数。
- **S:** 第2.1.6节 多头注意力, 第3.3.2节 LLaMA-2系列, 第5.1.3.1节 repeat_kv

### Q18
- **Q:** LoRA（Low-Rank Adaptation）是一种高效微调方法。请回答：LoRA基于的本征秩（intrinsic rank）假设是什么？LoRA如何应用于Transformer的注意力层？在RAG系统中，是否适合使用LoRA来微调检索模型或生成模型？请说明理由。
- **A:** 本征秩假设：预训练模型针对特定下游任务的权重更新矩阵具有较低的本征秩，即可以通过低秩分解 W = W0 + BA 来近似，其中 B ∈ R^{d×r}, A ∈ R^{r×k}，r ≪ min(d,k)。应用于注意力层：LoRA通常作用于注意力模块的 Wq, Wv 权重矩阵，冻结原权重，只训练低秩矩阵 A 和 B。前向计算为 h = W0·x + BA·x。RAG中使用LoRA：生成模型适合，RAG中的大语言模型负责生成答案，使用LoRA微调可以高效注入检索到的知识，且避免灾难性遗忘。检索模型（如Embedding模型）不适合，检索模型需要保持高质量的向量表示空间，LoRA的低秩约束可能损害表示能力，且检索模型通常不通过LoRA微调，而是采用对比学习。
- **S:** 第4.1.2节 涌现能力, 第4.2节 训练LLM, 第6.3.2节 LoRA微调, 第6.3.3节 LoRA原理, 第7.2节 RAG

### Q19
- **Q:** 文本表示方法从词向量（Word2Vec）发展到ELMo再到BERT，最后到LLM时代的大模型，其核心演进脉络是什么？在RAG系统中，为什么通常使用专门的Embedding模型（如BGE）而不是直接使用LLM作为检索编码器？
- **A:** 演进脉络：Word2Vec（2013）：静态词向量，无法处理一词多义。ELMo（2018）：动态词向量（双向LSTM），引入预训练思想，但仍是特征融合。BERT（2018）：Transformer编码器，MLM预训练，产生上下文相关的表示，成为NLU标杆。LLM（2020+）：Decoder-only结构，CLM预训练，涌现能力，可用于生成和理解。RAG中为何不直接用LLM作为检索编码器：效率上LLM参数量大（7B+），编码全部文档成本极高；专用Embedding模型（如BGE-large仅0.3B）更快。表示目的不同：LLM的表示偏向生成，不适合做相似度检索；Embedding模型经过对比学习训练，使相关文本在向量空间中更接近。解耦：检索和生成分离可独立优化，支持动态更新知识库而无需重训LLM。
- **S:** 第1.4节 文本表示的发展历程, 第3.1节 Encoder-only PLM, 第4.1节 什么是LLM, 第7.2.1节 RAG基本原理

### Q20
- **Q:** 假设你需要为金融领域的问答系统微调一个LLM，但标注数据只有500条指令-回复对，且算力有限（单卡24GB显存）。请设计一套训练方案，包括：选择哪种微调方法？为什么？是否需要预训练？为什么？若后续需要引入实时财报数据，你会采用RAG还是继续微调？请比较两者的优劣。
- **A:** 微调方法：采用LoRA（秩r=8或16），并配合Q-LoRA（4bit量化）。原因：全量微调需要>24GB显存，LoRA可降至约10-15GB，且在小数据集上不易过拟合。不需要预训练，直接使用开源的金融预训练模型（如BloombergGPT、FinBERT）或通用LLM（如Qwen-7B）。预训练需要海量数据和算力，不现实。采用RAG，而不是持续微调。RAG优势：知识实时更新（财报发布后立即索引），可溯源，幻觉低；无需频繁重训。微调劣势：新知识需重新训练，且可能遗忘旧知识（灾难性遗忘）。最佳实践：基座模型用LoRA微调领域指令遵循能力，配合RAG检索最新数据。
- **S:** 第4.2.2节 SFT, 第6.3节 高效微调, 第7.2节 RAG

---

## 无关问题

### Q21
- **Q:** DeepSeek-V3的多头潜在注意力（MLA）机制是如何工作的？
- **A:** 文档中未提及
- **S:**

### Q22
- **Q:** 如何使用vLLM框架部署和加速大模型推理？
- **A:** 文档中未提及
- **S:**

### Q23
- **Q:** Mamba架构的选择性状态空间模型（SSM）相比Transformer有什么优势？
- **A:** 文档中未提及
- **S:**

### Q24
- **Q:** 为什么GPT被Claude模型超越？
- **A:** 文档中未提及
- **S:**

### Q25
- **Q:** 为什么openclaw爆火？
- **A:** 文档中未提及
- **S:**
